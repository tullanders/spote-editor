import type { FC } from 'react'
import type { SpoteEditorProps } from './SpoteEditor.types'

export type { SpoteEditorProps }

export const SpoteEditor: FC<SpoteEditorProps> = (_props) => {
  return null
}
